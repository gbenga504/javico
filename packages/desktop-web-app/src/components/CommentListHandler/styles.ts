import { makeStyles } from "@material-ui/core";
import { color } from "@javico/common/lib/design-language/Css";

export const useStyles = makeStyles({
  editPanelInput: {
    color: color.white
  },
  editPanelCancelButton: {
    color: color.white
  }
});
